import { M as Message, T as Task } from './a2a-BChW6W9V.cjs';
export { n as APIKeySecurityScheme, o as AgentCapabilities, A as AgentCard, p as AgentCardSignature, q as AgentCard_SecuritySchemesEntry, r as AgentExtension, l as AgentInterface, s as AgentProvider, t as AgentSkill, u as Artifact, v as AuthenticationInfo, w as AuthorizationCodeOAuthFlow, x as AuthorizationCodeOAuthFlow_ScopesEntry, C as CancelTaskRequest, y as ClientCredentialsOAuthFlow, z as ClientCredentialsOAuthFlow_ScopesEntry, D as DeleteTaskPushNotificationConfigRequest, B as DeviceCodeOAuthFlow, E as DeviceCodeOAuthFlow_ScopesEntry, G as GetExtendedAgentCardRequest, e as GetTaskPushNotificationConfigRequest, b as GetTaskRequest, H as HTTPAuthSecurityScheme, I as ImplicitOAuthFlow, F as ImplicitOAuthFlow_ScopesEntry, f as ListTaskPushNotificationConfigsRequest, g as ListTaskPushNotificationConfigsResponse, L as ListTasksRequest, c as ListTasksResponse, J as MessageFns, K as MutualTlsSecurityScheme, O as OAuth2SecurityScheme, N as OAuthFlows, P as OpenIdConnectSecurityScheme, Q as Part, R as PasswordOAuthFlow, U as PasswordOAuthFlow_ScopesEntry, V as Role, W as SecurityRequirement, X as SecurityRequirement_SchemesEntry, Y as SecurityScheme, Z as SendMessageConfiguration, S as SendMessageRequest, m as SendMessageResponse, a as StreamResponse, _ as StringList, h as SubscribeToTaskRequest, j as TaskArtifactUpdateEvent, d as TaskPushNotificationConfig, k as TaskState, $ as TaskStatus, i as TaskStatusUpdateEvent, a0 as protobufPackage, a1 as roleFromJSON, a2 as roleToJSON, a3 as taskStateFromJSON, a4 as taskStateToJSON } from './a2a-BChW6W9V.cjs';
export { a as A2A_CONTENT_TYPE, b as A2A_PROTOCOL_VERSION, c as A2A_VERSION_HEADER, d as AGENT_CARD_PATH, H as HTTP_EXTENSION_HEADER } from './constants-PdvgZRkF.cjs';
export { a as ExtensionURI, E as Extensions } from './extensions-DcJm6niD.cjs';
export { A as AgentCardSignatureGenerator, a as AgentCardSignatureVerifier, c as canonicalizeAgentCard, g as generateAgentCardSignature, v as verifyAgentCardSignature } from './signature-CLKgbgG0.cjs';
import 'jose';

/**
 * Shared Server-Sent Events (SSE) utilities for both JSON-RPC and REST
 * transports.
 */
/** Standard HTTP headers for SSE streaming responses. */
declare const SSE_HEADERS: {
    readonly 'Content-Type': "text/event-stream";
    readonly 'Cache-Control': "no-cache";
    readonly Connection: "keep-alive";
    readonly 'X-Accel-Buffering': "no";
};
/** A parsed SSE event with type and data. */
interface SseEvent {
    type: string;
    data: string;
}
/**
 * Formats a data event for the SSE protocol.
 *
 * @example
 * ```ts
 * formatSSEEvent({ kind: 'message', text: 'Hello' })
 * // "data: {\"kind\":\"message\",\"text\":\"Hello\"}\n\n"
 * ```
 */
declare function formatSSEEvent(event: unknown): string;
/**
 * Formats an error event for the SSE protocol, using the `error` event
 * type so clients can distinguish errors from data events.
 *
 * @example
 * ```ts
 * formatSSEErrorEvent({ code: -32603, message: 'Internal error' })
 * // "event: error\ndata: {\"code\":-32603,\"message\":\"Internal error\"}\n\n"
 * ```
 */
declare function formatSSEErrorEvent(error: unknown): string;
/**
 * Parses an SSE stream from a `Response`, yielding events as they arrive.
 * Expects well-formed SSE events with single-line JSON data, matching the
 * format produced by {@link formatSSEEvent} and {@link formatSSEErrorEvent}.
 *
 * @param maxEventSizeBytes - Aborts the stream if a single line or event
 *   exceeds this size, bounding memory against a hostile server. Defaults to
 *   {@link DEFAULT_MAX_SSE_EVENT_SIZE_BYTES}.
 */
declare function parseSseStream(response: Response, maxEventSizeBytes?: number): AsyncGenerator<SseEvent, void, undefined>;

/**
 * Exports the common types.
 *
 * Use `./client` for the client-only codebase and `./server` for the
 * server-only codebase.
 */

type SendMessageResult = Message | Task;

export { Message, SSE_HEADERS, type SendMessageResult, type SseEvent, Task, formatSSEErrorEvent, formatSSEEvent, parseSseStream };
